CREATE TABLE department (
    id INT PRIMARY KEY department_id name VARCHAR(30)
) CREATE role (
    id INT title VARCHAR(30) salary DECIMAL department_id INT
) CREATE employee (
    id INT first_name VARCHAR(30) last_name VARCHAR(30) role_id INT
)